apiVersion: v1
kind: Service
metadata:
  name: {{ include "health-diary.fullname" . }}
  labels:
    {{- include "health-diary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "health-diary.selectorLabels" . | nindent 4 }}
