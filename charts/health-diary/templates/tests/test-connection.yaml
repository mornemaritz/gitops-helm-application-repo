apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "health-diary.fullname" . }}-test-connection"
  labels:
    {{- include "health-diary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "health-diary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
